source 'https://cdn.cocoapods.org/'

platform :ios, '10.0'

target 'Test iOS project' do
    pod 'Lynx', '3.2.0-rc.0', :subspecs => [
        'Framework',
    ]

    pod 'PrimJS', '2.11.1-rc.0', :subspecs => ['quickjs', 'napi']
end
